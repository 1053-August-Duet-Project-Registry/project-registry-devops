apiVersion: v1
kind: Service
metadata:
  name: {{ include "t1-account.fullname" . }}
  labels:
    {{- include "t1-account.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t1-account.selectorLabels" . | nindent 4 }}
