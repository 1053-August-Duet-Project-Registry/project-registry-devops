apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "t1-account.fullname" . }}-test-connection"
  labels:
    {{- include "t1-account.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "t1-account.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
